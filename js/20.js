/* 
    Funciones que retornan un valor
*/
function sumar(n1, n2) {
    return n1 + n2;
}

const resultado = sumar(2, 3);

console.log(resultado);

/* 
    esta funcion genera un valor, que es
    atrapado en una constante, por medio
    de llamar a la funcion con argumentos
    despues se puede mostrar por consola
    con el conole.log
*/

